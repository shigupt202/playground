name: CI
on: [push]
jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v1
      
#     - name: Run a multi-line script
#       run: |
#         docker pull nginx
        
#     - uses: Azure/k8s-set-context@v1
#       with:
#         kubeconfig: ${{ secrets.DIVMAN_KUBE_CONFIG }}

#     - uses: Azure/k8s-deploy@v1
#       name: Kubernetes deploy
#       with:
#         namespace: default
#         manifests: |
#           deployment.yaml
#           service.yaml
#         strategy: canary
#         traffic-split-method: pod
#         percentage: 50

    - uses: shigupt202/local-container-scan@staging
      name: Container scan
      with:
        imageName: shivamg202/phippy:test
        username: shivamg202 
        password: ${{ secrets.PASSWORD }}
        whitelist-file: whitelist
        github-token: ${{ secrets.GITHUB_TOKEN }}
   
   
    - name: View Whitelist file
      run: |
        cd /home/runner/work/playground/playground/_temp/containerScanWhitelist
        ls
        cat whitelist
