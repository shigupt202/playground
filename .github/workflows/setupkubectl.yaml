name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest       
    steps:
    - uses: actions/checkout@v1  
    - uses: azure/setup-kubectl@dependabot/npm_and_yarn/actions/core-1.2.6-releases
      with:
        version: 'v1.14.8'  
    - run: 'kubectl version'

