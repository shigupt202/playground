name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest       
    steps:
    - uses: actions/checkout@v1   
    - run: |
        echo "${{ github.workspace }}/values.yaml" >> $GITHUB_PATH 
    - run: |
        echo $GITHUB_PATH
        cat $GITHUB_PATH

