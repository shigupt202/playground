name: CI
on: 
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest       
                            
    steps:      
    - uses: actions/checkout@v1      
    - uses: azure/container-scan@users/shigupt/fixNodeNotifierJestAlert
      name: Container scan
      with:
        image-name: nginx
        add-CIS-checks: True 
#         username: shivamg202
#         password: ${{ secrets.PASSWORD }}
        severity-threshold: LOW                                      
          
