name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest       
    steps:
    - uses: actions/checkout@v1   
    - run: |
        cat ${{ github.workspace }}/values.yaml
    - run: |
        echo $GITHUB_PATH
        cat $GITHUB_PATH

