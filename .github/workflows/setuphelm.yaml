name: Setup Helm
on: push
jobs:
  build:
    runs-on: ubuntu-latest       
    steps:
    - uses: actions/checkout@v1  
    - uses: azure/setup-helm@v1.1
      with:
        version: 'latest' 
    - uses: azure/setup-helm@v1.1
      with:
        version: '2.*' 
    - uses: azure/setup-helm@v1.1
      with:
        version: '3.*'  
    - uses: azure/setup-helm@v1.1
      with:
        version: 'v3.4.1' 
