name: Setup Helm
on: push
jobs:
  build:
    runs-on: ubuntu-latest       
    steps:
    - uses: actions/checkout@v1  
    - uses: azure/setup-helm@users/shigupt/addingGraphQLForVersions
      with:
        version: 'latest' 
    - uses: azure/setup-helm@users/shigupt/addingGraphQLForVersions
      with:
        version: '2.*' 
    - uses: azure/setup-helm@users/shigupt/addingGraphQLForVersions
      with:
        version: '3.*'  
    - uses: azure/setup-helm@users/shigupt/addingGraphQLForVersions
      with:
        version: 'v3.4.1' 
    env:
      NEW_VERSION_LOGIC: 'true'
